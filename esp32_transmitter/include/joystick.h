#ifndef JOYSTICK_H
#define JOYSTICK_H

#include <stdint.h>
#include <stdbool.h>

// Declare joystick helper functions or types here, for example:
// uint16_t read_joystick_x(void);
// uint16_t read_joystick_y(void);

#endif // JOYSTICK_H
